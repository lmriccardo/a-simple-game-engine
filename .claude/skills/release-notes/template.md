# Release Notes — vX.Y.Z [YYYY-MM-DD]

<One short paragraph. What this release adds. If it completes a roadmap
milestone, name the arc: what this release finishes and what the release(s)
before it already contributed.>

## Highlights

- **<Feature name>** — <what it is, the new types/methods in `backticks`,
  and the one-clause reason it matters. No build-order narration, no bug
  backstory.>
- **<Feature name>** — <...>
- **New `<example>_demo` example** — <what it exercises end-to-end, in one
  sentence.>

## Testing

- **<Category> coverage** — <what's covered, at the level of "what
  scenarios", not "which asserts">.
- **<Pixel-verified / integration> proof** — <only if there's a real
  headless/end-to-end check worth calling out>.
- **Full suite status** — <pass count>/<total>, if notable.

## Contributors

- <one per line, from `git log <prev-tag>..<new-tag> --format=%an | sort -u`,
  human authors only>
